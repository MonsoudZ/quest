{
  "name": "signal-quest",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node server.mjs",
    "test": "node --test tests/*.test.mjs",
    "check": "for f in dist/*.js; do node --check \"$f\" || exit 1; done"
  }
}
